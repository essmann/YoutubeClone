import React from 'react'
import { useNavigate } from 'react-router-dom'
import {Paper, IconButton} from "@mui/icons-material"
import {SearchIcon} from "@mui/icons-material"

const Searchbar = () => {
  return (
    <div>
      searchbar
    </div>
  )
}

export default Searchbar
